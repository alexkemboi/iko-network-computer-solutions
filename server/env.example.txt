# ─────────────────────────────────────────────────────────────────────────────
# IKONEX API — copy this file to server/.env and fill in your values.
# server/.env is git-ignored: your keys never go to GitHub or the browser.
# ─────────────────────────────────────────────────────────────────────────────

# Where the API listens (nginx forwards https://yourdomain/api/ → here)
PORT=5050
HOST=127.0.0.1
# Your website address (used for CORS). Same-domain setups can leave "*".
ALLOWED_ORIGIN=https://ikonexsystems.com

# ── M-Pesa Express (Lipa Na M-Pesa Online) — PRODUCTION ─────────────────────
MPESA_ENV=production

# Daraja production endpoints (defaults shown — only change if Safaricom does)
MPESA_OAUTH_URL=https://api.safaricom.co.ke/oauth/v1/generate
MPESA_STKPUSH_URL=https://api.safaricom.co.ke/mpesa/stkpush/v1/processrequest
MPESA_STKQUERY_URL=https://api.safaricom.co.ke/mpesa/stkpushquery/v1/query

# From your approved production app on https://developer.safaricom.co.ke
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_PASSKEY=
MPESA_SHORTCODE=

# Paybill number  → CustomerPayBillOnline  (leave MPESA_PARTY_B empty)
# Till / Buy Goods → CustomerBuyGoodsOnline and set MPESA_PARTY_B to the Till
#                    number (MPESA_SHORTCODE is then your Store / HO number)
MPESA_TRANSACTION_TYPE=CustomerPayBillOnline
MPESA_PARTY_B=

# Optional: fixed "Account Number" customers see on the Paybill prompt.
# Leave empty to use a short per-item reference (e.g. IKX-SCHOOLFE).
MPESA_ACCOUNT_REFERENCE=

# Public HTTPS URL Safaricom calls with the result. Must reach this server.
MPESA_CALLBACK_URL=https://ikonexsystems.com/api/mpesa/callback
# A long random string; it's appended to the callback URL so only Safaricom
# (who is given that exact URL) can report payments. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
MPESA_CALLBACK_SECRET=

# Largest amount a customer can pay in one go (KES)
MPESA_MAX_AMOUNT=250000

# ── Database: SQL Server (e.g. SQL Server Express on this PC) ───────────────
# Create the login first with server/db/create-login.sql, then run: npm run db:setup
DB_SERVER=localhost
DB_INSTANCE=SQLEXPRESS
# Use DB_PORT instead of DB_INSTANCE if SQL Server Browser isn't running (e.g. 1433)
DB_PORT=
DB_NAME=ikonex
DB_USER=ikonex_app
DB_PASSWORD=
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# Session signing key for logins (optional — generated automatically if empty)
AUTH_SECRET=

# ── Bulk SMS: Africa's Talking ───────────────────────────────────────────────
AT_ENV=production
AT_USERNAME=
AT_API_KEY=
# Registered sender ID / shortcode (leave empty to use Africa's Talking's default)
AT_SENDER_ID=
# Delivery reports: in the AT dashboard set the SMS delivery callback to
#   https://ikonexsystems.com/api/sms/delivery/<MPESA_CALLBACK_SECRET>

# ── Bulk payments: M-Pesa B2C ────────────────────────────────────────────────
# If your B2C product is on a different Daraja app, set its own key/secret:
MPESA_B2C_CONSUMER_KEY=
MPESA_B2C_CONSUMER_SECRET=
MPESA_B2C_SHORTCODE=
MPESA_B2C_INITIATOR=
# The encrypted initiator password ("Security Credential") generated on the Daraja portal
MPESA_B2C_SECURITY_CREDENTIAL=
# BusinessPayment | SalaryPayment | PromotionPayment
MPESA_B2C_COMMAND_ID=BusinessPayment
MPESA_B2C_RESULT_URL=https://ikonexsystems.com/api/mpesa/b2c/result
MPESA_B2C_TIMEOUT_URL=https://ikonexsystems.com/api/mpesa/b2c/timeout
MPESA_B2C_MAX_AMOUNT=150000
